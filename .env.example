# Copy to .env and fill in real values. Never commit .env to git.

DATABASE_URL="mysql://user:password@localhost:3306/zoomcollege"

# Optional: clone local DB to VPS
# TARGET_DATABASE_URL="mysql://user:password@your-vps-ip:3306/zoomcollege"

CLOUD_NAME=""
API_KEY=""
API_SECRET=""
JWT_SECRET_KEY=""

# AI Insights (server only — add on VPS .env too)
OPENAI_API_KEY=""
# OPENAI_MODEL=gpt-4o-mini
